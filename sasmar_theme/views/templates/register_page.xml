<?xml version="1.0" encoding="utf-8"?>
<odoo>
<data>

<!-- Add Extra Field in Registration page -->
<template id="registration_form_extend" inherit_id="auth_signup.fields" customize_show="True" name="Add Extra Fields on Registration Page">

<xpath expr="//div[@class='form-group field-login']" position="before">

             <div class="form-group field-type-of-registration" style="display:none;">
                                  <label class="control-label" for="type">Type of Registration</label>
                                  <select id="type" name="type" class="form-control">
                                      <option value="consumer">Consumer</option>
                                      <option value="wholesaler">Wholesaler</option>
                                  </select>
                                  
            </div>
  </xpath>
  
  </template>
  
  <!--
   		<template id="auth_signup.fields" name="Auth Signup/ResetPassword form fields">
			
			<div class="form-group field-type-of-registration">
                                  <label class="control-label" for="type">Type of Registration</label>
                                  <select id="type" name="type" class="form-control" onchange="optionCheck()">
                                      <option value="consumer">Consumer</option>
                                      <option value="wholesaler">Wholesaler</option>
                                  </select>
                                  
            </div>
            
            <div id="your_email" class="form-group field-login">
                <label for="login" class="control-label">Your Email</label>
                <input type="text" name="login" t-att-value="login" id="login" class="form-control" autofocus="autofocus"
                    required="required" t-att-readonly="'readonly' if only_passwords else None"/>
            </div>

            <div id="your_name" class="form-group field-name">
                <label for="name" class="control-label">Your Name</label>
                <input type="text" name="name" t-att-value="name" id="name" class="form-control" placeholder="e.g. John Doe"
                    required="required" t-att-readonly="'readonly' if only_passwords else None"
                    t-att-autofocus="'autofocus' if login and not only_passwords else None" />
            </div>

            <div id="your_password" class="form-group field-password">
                <label for="password" class="control-label">Password</label>
                <input type="password" name="password" id="password" class="form-control"
                    required="required" t-att-autofocus="'autofocus' if only_passwords else None"/>
            </div>

            <div id="confirm_password" class="form-group field-confirm_password">
                <label for="confirm_password" class="control-label">Confirm Password</label>
                <input type="password" name="confirm_password" id="confirm_password" class="form-control" required="required"/>
            </div>
        </template>
		-->
       



</data>
</odoo>

